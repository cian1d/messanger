meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10

build:
  requirementsPath: requirements.txt

run:
  script: python app.py
  persistence:
    - mountPath: /data
      name: messages-storage